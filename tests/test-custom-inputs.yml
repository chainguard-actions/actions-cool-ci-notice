name: Test custom notice inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          token: ${{ github.token }}
          ci: echo build-complete
          notice-title: "CI Notice Test"
          notice-body: "This is a test notification body"
      - name: Assert result is success with custom inputs
        env:
          RESULT: ${{ steps.action.outputs.result }}
        run: |
          echo "result: $RESULT"
          if [ "$RESULT" != "success" ]; then
            echo "FAIL: expected result='success' but got '$RESULT'"
            exit 1
          fi
          echo "PASS: result='$RESULT'"
